import Head from "next/head";
import NextLink from "next/link";

const NotFound = (props) => (
  <>
    <Head>
      <title>404 | BecomTik</title>
    </Head>
    404: The page you are looking for isn’t here
  </>
);

export default NotFound;
